
class TokenExpiredError {
    static async checkToken(token, idUser, nick) {
        // Implemente a lógica para verificar o token
        // Retorne true se o token for válido, ou false se expirado
        return true; // Exemplo de retorno, ajuste conforme necessário
    }
}

module.exports = TokenExpiredError;